{
  "name": "ff-one-pager",
  "version": "1.0.0",
  "description": "Browser extension one-pager with Vue.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean": "rm -rf dist",
    "build": "npm run clean && vite build",
    "preview": "vite preview",
    "web-ext:run": "npm run build:firefox && web-ext run --source-dir=dist --no-input",
    "web-ext:lint": "web-ext lint --source-dir=dist",
    "build:firefox": "npm run clean && TARGET_BROWSER=firefox npm run build",
    "build:chrome": "npm run clean && TARGET_BROWSER=chrome npm run build",
    "build:pwa": "npm run clean && VITE_PWA=true vite build && mv dist/index.pwa.html dist/index.html",
    "release:firefox": "npm run build:firefox && web-ext build --source-dir=dist --artifacts-dir=web-ext-artifacts --overwrite-dest",
    "release:chrome": "npm run build:chrome && web-ext build --source-dir=dist --artifacts-dir=web-ext-artifacts --overwrite-dest",
    "test": "vitest"
  },
  "dependencies": {
    "@toast-ui/editor": "^3.2.2",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "@vue/test-utils": "^2.4.6",
    "jsdom": "^27.0.0",
    "vite": "^5.4.20",
    "vite-plugin-pwa": "^1.0.0",    
    "vite-plugin-static-copy": "^3.1.4",
    "vitest": "^3.2.4",
    "web-ext": "^9.0.0"
  }
}
